<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/style.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Użytkownicy</title>
</head>

<body>
    <div class="topnav">
        <p>Sklep internetowy</p>
        <% if( user && user.user_type ) {%>
            <a href="/logout">Wyloguj</a>
            <%} else {%>
                <a href="/log_in">Logowanie</a>
                <% } %>
                    <a href="/items">Oferta</a>
                    <a href="/">Strona główna</a>
    </div>


    <form method="post">
        <div class="search-container">
            <input type="text" placeholder="Wyszukaj login lub imię i nazwisko" name="search" value="<%=search %>">
            <button type="submit"><i class="material-icons">search</i></button>
        </div>
        <div style="column-count: 2; width: 40%; margin-left: 30%;">
            <select style="font-size: medium; height: 30pt; width:220pt;" name="usertype" value="<%=usertype %>">
                <option value="">Wszyscy</option>
                <option value="user">User</option>
                <option value="admin">Admin</option>
            </select>
            <button style="font-size: medium; margin-top: 14pt; height: 30pt; width:220pt;" type="submit">Szukaj</button>
        </div>
    </form>


    <div>
        <table class="items" style="margin-top: 5em;">
            <tr>
                <th>Login</th>
                <th>Imię</th>
                <th>Nazwisko</th>
                <th>Typ użytkownika</th>
            </tr>
            <% users.forEach(r=> { %>
                <tr>
                    <td>
                        <%= r.login %>
                    </td>
                    <td>
                        <%= r.user_name %>
                    </td>
                    <td>
                        <%= r.user_surname %>
                    </td>
                    <td>
                        <%= r.user_type %>
                    </td>
                </tr>
                <% }) %>
        </table>
    </div>

</body>

</html>